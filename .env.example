# Clave secreta de Stripe (modo test/sandbox)
# Opcional: si se configura aquí, se usa como fallback cuando no se introduce
# la clave en la interfaz. En la app desplegada, los usuarios introducen
# su propia clave directamente en el navegador.
# https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
